import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 28,
    textAlign: 'center',
    width: '100%',
    fontWeight: 'bold',
    color: 'black',
    marginTop: 55,
  },
})

export default styles;